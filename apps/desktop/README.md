# ReiLink Desktop

Electron + React renderer for the ReiLink Phase 1 desktop MVP.

## Run

```bash
cd apps/desktop
npm install
npm run dev
```

For Electron:

```bash
npm run dev:electron
```

The renderer expects the backend at `http://127.0.0.1:8000`. Override with `VITE_REILINK_API_BASE`.

